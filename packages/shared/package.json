{
  "name": "@convozy/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Types, DTOs, and constants shared between apps/api and apps/web",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.70.1"
  }
}
